[project]
name = "morenatsu-translate-tools"
version = "0.1.0"
description = "Text extraction and embedding tools for Morenatsu Revival CN translation"
requires-python = ">=3.11"
dependencies = [
    "openai>=1.0.0",
    "numpy>=1.24.0",
]

[tool.uv]
dev-dependencies = []
